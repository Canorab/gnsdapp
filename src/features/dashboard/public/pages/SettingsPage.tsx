/**
 * A page to display some settimghs fields and forms for the currently logged in user.
 */

import React from 'react';

function SettingsPage() {
	return (
		<div>
			<h1 className='text-cyan-600'>Settings</h1>
		</div>
	);
}

export default SettingsPage;
