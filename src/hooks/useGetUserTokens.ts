/**
 * Receives; Token contractAddress, Token type (ERC20, RERC731) and user's walletAddress
 * use the received arguments to call Moralis sdk or web3js to connect to the blockchain and find the user's token list
 * Returns all found tokens in json format
 */
